import { CovalentTopAppBarFixedBase } from './top-app-bar-fixed';

describe('Top App Bar Fixed', () => {
  it('should work', () => {
    expect(new  CovalentTopAppBarFixedBase()).toBeDefined();
  });
});
