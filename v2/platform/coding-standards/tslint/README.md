# Covalent tslint config

Create a `tslint.json` file at the root of your project with the following content:

```json
{
  "extends": ["./node_modules/@covalent/coding-standards/tslint/tslint.js"]
}
```
